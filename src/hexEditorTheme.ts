import defaultTheme from "react-hex-editor/themes";

const theme = {
  ...defaultTheme,
  rowPaddingY: 2,
  bytePaddingX: 5,
  colorBackgroundCursor: "rgba(214, 217, 220, .75)",
  colorBackgroundCursorHighlight: "rgba(214, 217, 220, .75)",
};

export default theme;
